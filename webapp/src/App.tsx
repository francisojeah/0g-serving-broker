import { FC, ReactElement } from "react";
import AgLayout from "./pages/layout/layout";

const App: FC = (): ReactElement => {
  return (
    <div className="App">
      <AgLayout />
    </div>
  );
};

export default App;
